/* eslint-disable unicorn/prevent-abbreviations */
/// <reference types="vite/client" />

/**
 * Which edition this bundle is, decided by the build and inlined as a literal
 * (see `define` in vite.config.mts). `false` in this plugin's own build.
 */
declare const BIT_CONNECT_PRO_BUILD: boolean
