export const store = () => ({ actions: {}, state: {} })
export const getServerState = () => {
  // The interactivity runtime is absent in tests; there is no server state.
}
export const getContext = () => {
  // Likewise: no directive context outside the runtime.
}
